export const metadata = {
  title: 'Blog - DuckFoo',
  description: 'Latest insights and updates from the DuckFoo team',
}

export default function BlogLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}

